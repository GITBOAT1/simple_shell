#include "shell.h"


/**
 * main - run out process
 *
 * Return: this return 0 on sucess.
 */


int main()
{
	int l = 0;
	while(1)
	{
		l = shell();
		if (l == 99)
			break;
		printf("value in l %d\n",l);
		sleep(3);
	}
	return (0);
}
